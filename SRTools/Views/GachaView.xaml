<Page
    x:Class="SRTools.Views.GachaView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SRTools.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid HorizontalAlignment="Stretch" Margin="24">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧区域 -->
        <StackPanel Grid.Column="0" HorizontalAlignment="Left" Orientation="Horizontal">
            <StackPanel HorizontalAlignment="Stretch">
                <ToggleButton x:Name="ProxyButton" Content="开启代理" Click="StartProxyButton_Click" Margin="0 0 0 8"/>
            </StackPanel>
        </StackPanel>

        <!-- 右侧区域 -->
        <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            
        </StackPanel>

        <TeachingTip x:Name="GachaLink"
            Target="{x:Bind ProxyButton}"
            Title="代理"
            Subtitle="SubTitle"
	        CloseButtonContent="关闭">
            <TeachingTip.IconSource>
                <SymbolIconSource Symbol="Stop" />
            </TeachingTip.IconSource>
        </TeachingTip>

    </Grid>
</Page>
