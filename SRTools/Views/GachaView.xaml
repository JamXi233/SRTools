<Page
    x:Class="SRTools.Views.GachaView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SRTools.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid HorizontalAlignment="Stretch" Margin="24">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧区域 -->
        <StackPanel Grid.Column="0" HorizontalAlignment="Left" Orientation="Horizontal">
            <StackPanel HorizontalAlignment="Stretch">
                <Button x:Name="StartProxyButton" Content="开启代理" Click="StartProxyButton_Click" Margin="0 0 0 8"/>
                <Button x:Name="StopProxyButton" Content="关闭代理" Click="StopProxyButton_Click" Margin="0 0 0 8"/>
            </StackPanel>
        </StackPanel>

        <!-- 右侧区域 -->
        <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top">
            <TextBlock x:Name="GachaLink_Box" Text="正在等待" TextWrapping="Wrap" Width="auto"/>
        </StackPanel>

        <TeachingTip x:Name="GachaLink"
            Target="{x:Bind StartProxyButton}"
            Title="代理"
            Subtitle="SubTitle"
	        CloseButtonContent="关闭">
            <TeachingTip.IconSource>
                <SymbolIconSource Symbol="Stop" />
            </TeachingTip.IconSource>
        </TeachingTip>
    </Grid>
</Page>
